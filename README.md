# Namaz Guide AI 🤖

Capstone-проект для курса AI Engineer, представляющий собой систему для анализа намаза с использованием компьютерного зрения. Приложение в реальном времени распознает позы молящегося, отслеживает их последовательность и автоматически подсчитывает ракааты.

---

## 🚀 Основные возможности

*   **Детекция поз:** Распознавание 4 ключевых поз (Стояние, Поясной поклон, Земной поклон, Сидение).
*   **Подсчет ракаатов:** Автоматический подсчет ракаатов на основе правильной последовательности земных поклонов.
*   **Веб-интерфейс:** Интерактивное приложение на Streamlit для легкой загрузки видео и демонстрации.
*   **Высокая производительность:** Используется легковесная модель YOLOv8n-Pose, что позволяет запускать приложение даже на слабых компьютерах.

---

## 🛠️ Технологический стек

*   **Python 3.12**
*   **Computer Vision:** OpenCV, Ultralytics YOLOv8
*   **Machine Learning:** Scikit-learn
*   **Web Framework:** Streamlit
*   **Data Handling:** Pandas, NumPy

---

## ⚙️ Как запустить локально

1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com/ВАШ_НИКНЕЙМ/Namaz-Guide-AI.git
    cd Namaz-Guide-AI
    ```

2.  **Создайте и активируйте виртуальное окружение:**
    ```bash
    # Для Windows
    python -m venv venv
    .\venv\Scripts\activate
    ```

3.  **Установите зависимости:**
    ```bash
    pip install -r requirements.txt
    ```

4.  **Добавьте видеофайл:**
    Поместите видеофайл с записью намаза в корневую папку проекта и убедитесь, что в файле `app.py` (или `live_predictor.py`) в переменной `VIDEO_PATH` указано правильное имя вашего файла.

5.  **Запустите Streamlit-приложение:**
    ```bash
    streamlit run app.py
    ```